// Category route (/category/[slug]) - insights filtered by one advisory
// category (e.g. market entry, regulatory, cross-border trade).
// Skeleton stage - not implemented yet.
// Will receive `params: { slug: string }` once implemented.

export default function CategoryPage() {
  return null;
}
